<div class="border-solid border-2 border-black lg:w-[30%]">
	<div class="flex flex-col space-y-5 items-center p-2 h-full justify-between">
		<div>
			<%- include('cardTitle', {
				title: title,
				wrapperClassName: 'text-center'
			}) %>
			<%- include('cardTitle', {
				title: "pour " + price + "/ mois",
				wrapperClassName: 'text-center'
			}) %>
		</div>
		<ul>
			<%- include('listElement', { type: '' }) %>
			<%- include('listElement', { type: 'check' }) %>
			<%- include('listElement', { type: '' }) %>
		</ul>
		<%- include('CTAButton', {
			label: "J'en profite",
			isHighLighted: isHighLighted,
			wrapperClassName: ''
		}) %>
	</div>
</div>
